name: ebook_reader
description: Leitor de ebooks
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.2
  permission_handler: ^10.2.0
  path_provider: ^2.0.8
  #sqflite: ^2.3.0 # TODO descomentar quando for iniciar o processo de favoritar.
  #vocsy_epub_viewer: ^2.0.0 ## Esse dá trampo de configurar. Entrar no site para olhar

  # TODO    precisar finalizar a função de salvar o arquivo e então dar continuidade

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg